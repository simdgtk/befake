
<div class="container fs-5">
    <div class="d-flex justify-content-between align-items-baseline">
      <%= link_to image_tag("arrow_left.svg", width:"40px", title:"Retour à l'accueil"), root_path %>
      <div class="d-flex flex-column">
          <p class="fw-bold fs-3 py-4">BeFake.</p>
      </div>
            <% 
            limit = Time.new(2024, 5, 16, 14, 35, 0)
            if @befakepost.created_at > limit %>
              <div>
        <%= button_to image_tag("bin.svg", width:"40px"), @befakepost, method: :delete, class:"destroy", style: 'background:none; border:none' %>

      </div>
      <% else %>
      <p></p>
      <% end %>
      
           <!--<%= link_to "Éditer le post", edit_befakepost_path(@befakepost), class: "text-white text-decoration-none fw-bold py-4" %> -->

  </div>

<div class="container d-flex flex-column justify-content-between">


  <%= render "form", befakepost: @befakepost %>
</div>

  <br>


</div>
